#include <iostream>
#include <chrono>
//#include "docparser.h"
//#include "referencestringcleaner.h"
//#include "indexinterface.h"
//#include "avlinterface.h"
#include "interface.h"

using namespace std;

int main(int argc, char** argv)
{
    //unsigned int totalSeconds = 0;
    Interface UI;
/*
    //DocParser wikiBooks(argv[1], argv[2]);
    std::chrono::time_point<std::chrono::system_clock> start, end;
    start = std::chrono::system_clock::now();
    //wikiBooks.parse();
    end = std::chrono::system_clock::now();
    unsigned int seconds = std::chrono::duration_cast<std::chrono::seconds>(end-start).count();
    totalSeconds += seconds;
    cout << "Parse took " << totalSeconds << " seconds" << endl;
    //wikiBooks.searchFor("firefight");
    cout << endl << "---------------------------------------------------" << endl;
    //wikiBooks.searchFor("computer");
    cout << endl << "---------------------------------------------------" << endl;
*/
}
